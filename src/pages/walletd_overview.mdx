import { Libraries } from '@/components/Libraries'


export const description = 'WalletD Library Overview.'

export const sections = [
  { title: 'Official Rust Libraries', id: 'official-libraries' },
]

# WalletD Overview

At its core, WalletD is an SDK designed to make interaction with various cryptocurrencies easier. 

Built in Rust, we plan to make our official crates available for use on multiple platforms through the use of WASM libraries.

The various official Rust crates that provide the core functionality for the WalletD ecosystem are listed below. {{ className: 'lead' }}

<Libraries />