import { Libraries } from '@/components/Libraries'

export const description =
  'Huh?'

export const sections = [
  { title: 'Official WalletD crates', id: 'official-libraries' },
]

# WalletD architectual overview

At its core, WalletD is an SDK designed to make interaction with various cryptocurrencies easier. 

The various official submodules that form the WalletD ecosystem are documented below. {{ className: 'lead' }}

This should be a table with an explanation of what each crate does 




<Libraries />
